# Legacy POC scripts (removed from history)
poc/

# Local secrets override (never commit)
.claude/secrets.local.op

# Test artifacts
tests/tmp/
tests/*.log
tests/*.tmp
# Wrapper copy used by the mock-integration test; must sit in bin/ so the
# wrapper's "<own dir>/../lib" resolves. Normally removed by a RETURN trap.
bin/.test-integration-wrapper.*

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Temporary files
*.bak
*.tmp
